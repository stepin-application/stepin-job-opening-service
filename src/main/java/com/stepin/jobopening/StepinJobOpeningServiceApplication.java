package com.stepin.jobopening;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application class for StepIn Job Opening Service.
 * This service manages job openings and their lifecycle within recruitment
 * campaigns.
 */
@SpringBootApplication
public class StepinJobOpeningServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(StepinJobOpeningServiceApplication.class, args);
    }
}
